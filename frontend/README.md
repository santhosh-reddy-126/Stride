# Task Manager UI

A simple, professional React frontend for the task manager application.

## Setup

1. Open the `frontend` folder.
2. Run `npm install`.
3. Run `npm run dev`.

## Notes

- This UI is intentionally lightweight and focuses on a clean, modern layout.
- The app can be configured to connect to a backend via `VITE_API_BASE`.
